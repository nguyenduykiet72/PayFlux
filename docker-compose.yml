services:
  etcd:
    image: bitnamilegacy/etcd:3.5.11
    container_name: payflux-etcd
    environment:
      - ALLOW_NONE_AUTHENTICATION=yes
      - ETCD_ADVERTISE_CLIENT_URLS=http://0.0.0.0:2379
    ports:
      - "2379:2379"
    networks:
      - payflux_network
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          memory: 256M
    healthcheck:
      test: ["CMD","etcdctl","endpoint","health"]
      interval: 10s
      timeout: 5s
      retries: 5

  apisix:
    image: apache/apisix:3.15.0-debian
    container_name: payflux-apigateway
    restart: always
    depends_on:
      etcd:
        condition: service_healthy
    volumes:
      - ./apisix_conf/config.yaml:/usr/local/apisix/conf/config.yaml:ro,z
    ports:
      - "9080:9080"
      - "9180:9180"
      - "9091:9091" # Prometheus metrics
    extra_hosts:
      - "host.docker.internal:host-gateway" # Local dev only
    networks:
      - payflux_network
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
        reservations:
          memory: 512M
    healthcheck:
      test: ["CMD-SHELL", "wget -qO /dev/null http://localhost:9080/ || exit 0"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  apisix-dashboard:
    image: apache/apisix-dashboard:3.0.1-alpine
    container_name: payflux-apisix-dashboard
    restart: always
    depends_on:
      apisix:
        condition: service_healthy
    volumes:
      - ./dashboard_conf/config.yaml:/usr/local/apisix-dashboard/conf/conf.yaml:ro,z
    ports:
      - "9000:9000"
    networks:
      - payflux_network

  postgres:
    image: postgres:17
    container_name: payflux-postgres
    command: ["postgres","-c", "wal_level=logical"]
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123456789
      POSTGRES_DB: payflux_db
    ports:
      - "5678:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db:/docker-entrypoint-initdb.d
    networks:
      - payflux_network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d payflux_db" ]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:8.0
    container_name: payflux-redis
#    env_file:
#      - .env
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - payflux_network
    volumes:
      - redis_data:/data

  kafka:
    image: apache/kafka:4.0.0
    hostname: kafka
    container_name: payflux-kafka
    user: "0:0"
    ports:
      - "9092:9092"
      - "9093:9093"
    environment:
      KAFKA_PROCESS_ROLES: "broker,controller"
      KAFKA_NODE_ID: 1
      KAFKA_BROKER_ID: 1
      CLUSTER_ID: "V-P3qjL-S-e6mhj-XB2Oyw"
      KAFKA_CONTROLLER_QUORUM_VOTERS: "1@kafka:9093"

      KAFKA_LISTENERS: INTERNAL://0.0.0.0:29092,EXTERNAL://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka:29092,EXTERNAL://localhost:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT,CONTROLLER:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER

      KAFKA_LOG_DIRS: "/tmp/kraft-data"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_NUM_PARTITIONS: 3
    volumes:
      - kafka_data:/tmp/kraft-data
    networks:
      - payflux_network

  debezium:
    image: quay.io/debezium/connect:2.7.3.Final
    container_name: payflux-debezium
    ports:
      - "8083:8083"
    environment:
      - BOOTSTRAP_SERVERS=kafka:29092
      - GROUP_ID=1
      - CONFIG_STORAGE_TOPIC=my_connect_configs
      - OFFSET_STORAGE_TOPIC=my_connect_offsets
      - STATUS_STORAGE_TOPIC=my_connect_statuses
      - KEY_CONVERTER=org.apache.kafka.connect.json.JsonConverter
      - VALUE_CONVERTER=org.apache.kafka.connect.json.JsonConverter
      - KEY_CONVERTER_SCHEMAS_ENABLE=false
      - VALUE_CONVERTER_SCHEMAS_ENABLE=false
    depends_on:
      - kafka
      - postgres
    networks:
      - payflux_network

  jaeger:
    image: jaegertracing/all-in-one:1.74.0
    container_name: payflux-jaeger
    ports:
      - "4317:4317"
      - "4318:4318"
      - "16686:16686"
    networks:
      - payflux_network

volumes:
  postgres_data:
  redis_data:
  kafka_data:

networks:
  payflux_network:
    driver: bridge